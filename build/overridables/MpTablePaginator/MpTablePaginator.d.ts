/// <reference types="react" />
import { MpTablePaginatorProps } from "../../types/Overridables";
export default function MpTablePaginator({ pageIndex, previousPage, itemCount, nextPage, pageCount, pageSize, loading, translations, selectedCount, }: MpTablePaginatorProps): JSX.Element;
